#!/usr/bin/env bash
# Display the PID and executable path of processes containing "bash"

for pid in /proc/[0-9]*; do
  exe=$(readlink "$pid/exe" 2>/dev/null)
  if [[ "$exe" == *bash* ]]; then
    echo "$(basename "$pid") $exe"
  fi
done

